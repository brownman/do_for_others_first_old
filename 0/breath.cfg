shopt -s expand_aliases
dir_self=`dirname $BASH_SOURCE`
num=`cat $dir_self/level`

msg="level is:"
toilet --gay "$msg"
figlet $num


file=$( ls -d $dir_self/PENDING/$num* )
cmd="source $file/breath.cfg"
echo $cmd
sleep 1
eval "$cmd"



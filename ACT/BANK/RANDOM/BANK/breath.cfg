#info: random cli stuff
#pending: show a git tipp
#imagine: the smallest tip and then another
steps(){
bash -c $dir_root/LIST/.indicator.cfg
sleep 4
source "$dir_root/LIST/trick.cfg"
every 2 "bash -c ~/JAIL/TREE/.robot.cfg"
every 10 "bash -c $dir_root/.test.sh"
}


#( set -e; steps )
#&& { steps; }
steps

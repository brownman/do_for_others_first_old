#!/bin/bash
echo hi
exiting

pushd `pwd`>/dev/null
path_self=`dirname $0`

dir=$path_self
ls -l ` 
file=$dir/list


if [ -f "$file" ];then
    echo 'list exist'
else
    reason_of_death  'no list in current directory' "$dir"
fi

popd>/dev/null

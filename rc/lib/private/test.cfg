self_test(){
    
file=${1:-''}

filename=`basedir $file`
dirname=`dirname $file`
if [ "$file" ];then
cmd="cat $file | grep \"\#\#\" | sed s/##//g > $dirname/about_$filename.cfg"
confirm "$cmd"
else
    echo 'supply a file'
    echo 'to create a .test.cfg'
fi
}


export -f self_test


use(){
    echo '____________________________________use()___'
    if [ $# -gt 0 ];then
        args=( "$@" )
        $dir_rc/library.sh "${args[@]}"
    else
        $dir_rc/library.sh 
    fi

}


export -f use

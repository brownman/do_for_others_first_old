
notify-send(){
    local util=`which notify-send`
        local arg1="$1"
        local arg2="$2"

        if [ "$GUI" = true ];then
            $util "$arg1" "$arg2"
        else
            echo "$arg1"
                echo "$arg2"
                fi
}

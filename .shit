
args1(){

if [ "$1" ];then
    script="$path/$1"

    echo "script: $script"
    if [ -f "$script" ];then
        echo "script exist, continue..."

    else
        echo "no such script"
        exit
    fi


    if [ "$2" ];then
        shift
        args=( "$@" )
    fi
else
    echo 'supply a script to wrap'
    exit
fi


}


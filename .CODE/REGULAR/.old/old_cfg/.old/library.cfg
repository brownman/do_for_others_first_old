

use(){

shopt -s expand_aliases
    echo '____________________________________use()___'
    if [ $# -gt 0 ];then
        args=( "$@" )
        $dir_lib/library.sh "${args[@]}"
    else
        $dir_lib/library.sh 
    fi

}

export dir_tmp=$dir_lib/tmp
export -f use

validate_root(){
    #info: validate $dir_root is set
    #check: [ ! -d $dir_root ]
    #details: check if $dir_root is set - it should be set by the installer
    print_func
    export dir_root=${dir_root:-$PWD}
    echo '[DIR_ROOT]'
    echo "$dir_root"
}


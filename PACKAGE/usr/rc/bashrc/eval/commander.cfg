commander(){
    echo "COMMANDER()"
    if [ $# -gt 0 ];then
        cmd="$@"
        echo -e "evaluating:\t\t $cmd"
        eval "$cmd"
    else
        echo "No arguments - no command"
    fi
}

export -f commander

random_line(){
    file="$1"
    line=''
    if [ -f "$file" ];then
        line=$(shuffle "$file" |  head -1 )
        if [  -n "$line" ];then
            notify-send "$line"
        fi
    else
        reason_of_death "no such file" "$file"
    fi
}
export -f random_line



source $dir_root/.TRICK/save.cfg
source $dir_root/.TRICK/proxy.cfg

source $dir_root/.TRICK/funcs.cfg
source $dir_root/.TRICK/env.cfg

source $dir_root/.TRICK/quick.cfg

present pv do for others first

str=$( cat $dir_root/genesis.yaml | head -2 )
present pv "$str"

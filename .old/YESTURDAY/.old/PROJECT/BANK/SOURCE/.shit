sourcing(){
    local file=$1
    if [ -f "$file" ];then
        echo -e "\t\t\t [sourcing] $file"
        source $file
    else
        echo 'not a file' "$file"
    fi
}
sourcing(){
    local file=$1
    if [ -f "$file" ];then
        echo -e "\t\t\t[sourcing] $file"
        source $file
    else
        echo 'not a file' "$file"
    fi
}


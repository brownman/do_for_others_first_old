#The general idea is to install virtualenv itself globaly, i.e. 
sudo easy_install virtualenv 
#or
sudo pip install virtualenv
#, but then create the actual virtual environment ("run virtualenv") locally.

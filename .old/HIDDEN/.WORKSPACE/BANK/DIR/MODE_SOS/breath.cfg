clear

    dir_self="$( cd `dirname $0`;echo `pwd`)"
    echo $dir_self
    pushd "$dir_self" >/dev/null
echo it is an  indepandant script - please use it only as MODE_SOS



step1(){

    echo one need to meditate in order to reach silence | pv -qL 10
}
step2(){
    echo the robot will remind you that everything is possible    
}


step3(){
    dir_self=`dirname $0` 
        file_list=$dir_self/list.txt
        while read line;do
str="$( echo  $line | cut -d'?' -f1)"
echo $str
                done<$file_list
}

steps(){
    step1
        step2
        step3
}

[ "$MODE_SOS" = true ] && steps 
#popd>/dev/null

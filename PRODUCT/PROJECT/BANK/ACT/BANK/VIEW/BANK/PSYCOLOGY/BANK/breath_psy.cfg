if [ $0 = bash];then
present pv do for others first
level=$( cat $dir_root/level )
present toilet "WoW $level"
proxy sleep 2
str=$( cat $dir_root/genesis.yaml | head -${level} | tail -1 )
present pv "$str"
fi
